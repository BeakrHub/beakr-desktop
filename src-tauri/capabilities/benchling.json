{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "benchling",
  "description": "Minimal capability for the remote benchling.com import window. The page-context gather script talks to the Rust app only via a localhost HTTP bridge (plain fetch), so this remote origin is granted NO Beakr IPC commands or events.",
  "windows": ["benchling"],
  "remote": {
    "urls": ["https://benchling.com/*", "https://*.benchling.com/*"]
  },
  "permissions": []
}
